import 'package:flutter/material.dart';

class NTColors {
  // Brand Colors
  static const Color primary = Color(0xFF0F172A); // Deep Navy
  static const Color secondary = Color(0xFF059669); // Emerald Green
  static const Color tertiary = Color(0xFFB59410); // Soft Gold
  
  // Surface Colors
  static const Color background = Color(0xFFF8F9FF);
  static const Color surface = Color(0xFFE5EEFF);
  static const Color surfaceHigh = Color(0xFFDCE9FF);
  static const Color surfaceHighest = Color(0xFFD3E4FE);
  static const Color surfaceLow = Color(0xFFEFF4FF);
  static const Color surfaceLowest = Color(0xFFFFFFFF);
  
  // Content Colors
  static const Color onBackground = Color(0xFF0B1C30);
  static const Color onSurface = Color(0xFF0B1C30);
  static const Color onSurfaceVariant = Color(0xFF45464D);
  static const Color outline = Color(0xFF76777D);
  static const Color outlineVariant = Color(0xFFC6C6CD);
  
  // Status Colors
  static const Color error = Color(0xFFBA1A1A);
  static const Color success = Color(0xFF059669);
  static const Color warning = Color(0xFF725C00);
}

class NTSpacing {
  static const double base = 8.0;
  static const double xs = 4.0;
  static const double sm = 12.0;
  static const double md = 16.0;
  static const double lg = 24.0;
  static const double xl = 32.0;
  static const double gutter = 24.0;
  static const double marginMobile = 16.0;
}

class NTRadius {
  static const double sm = 4.0;
  static const double md = 8.0;
  static const double lg = 12.0;
  static const double xl = 16.0;
  static const double full = 999.0;
}
